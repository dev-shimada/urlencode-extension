{
  "name": "url-encoder-extension",
  "version": "1.0.0",
  "description": "Chrome extension to URL encode/decode selected text in URLs",
  "scripts": {
    "build": "tsx scripts/generate-png-icons.ts && tsc && tsx scripts/build.ts",
    "watch": "tsc --watch",
    "dev": "tsx scripts/generate-png-icons.ts && tsc && tsx scripts/build.ts",
    "package": "mkdir -p release && rm -f release/url-encoder-extension.zip && cd dist && zip -r ../release/url-encoder-extension.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^25.3.5",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3"
  }
}
